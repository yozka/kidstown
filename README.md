# kidstown
C++ small library for education

Пример использование библиотеки

#include "kidstown.h"
void main()
{
	print("Привет мир!");
	for (int i = 0; i < 500; i++)
  {
    robot.move(i, 100);
  }
}
